import { Theme, createStyles } from '@material-ui/core';


const SearchListVouchersStyle = (theme: Theme) => createStyles({
    tableContainer: {
        height: 400,
        marginTop: 0,
        backgroundColor: "white"
    },
});

export default SearchListVouchersStyle;